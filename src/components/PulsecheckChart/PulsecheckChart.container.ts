import { connect } from 'react-redux'
import PulsecheckChart from './PulsecheckChart.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(PulsecheckChart)
