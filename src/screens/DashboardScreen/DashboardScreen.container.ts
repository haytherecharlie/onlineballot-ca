import { connect } from 'react-redux'
import DashboardScreen from './DashboardScreen.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(DashboardScreen)
