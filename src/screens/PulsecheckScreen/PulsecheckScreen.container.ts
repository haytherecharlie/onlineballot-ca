import { connect } from 'react-redux'
import PulsecheckScreen from './PulsecheckScreen.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(PulsecheckScreen)
